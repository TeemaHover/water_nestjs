export enum UserType {

  business = 'business',

  panelist = 'panelist',

  user = 'user',

  // system
  system = 'system'
}

export enum UserStatus {
 
  pending = "pending",
 
  active = 'active',

  banned = 'banned'
}
